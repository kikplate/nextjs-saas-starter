# PostgreSQL (Prisma). Example matches docker-compose `db` service.
DATABASE_URL=postgresql://app:app@localhost:5433/app?schema=public

# Next.js (optional; defaults shown)
# NEXT_PUBLIC_APP_URL=http://localhost:3000
